import { DefaultTokens } from "@openfun/cunningham-react";

const config: DefaultTokens = {
  theme: {
    colors: {
      "primary-500": "purple",
    },
  },
  components: {
    button: {
      "border-radius": "30px",
    },
  },
};

export default config;
